//Superpower of React :-
// ==> React can efficiently find the difference between two Virtual Doms and then update this difference to the UI
// ==> Hence it has an efficient diff algorithm which can do efficient DOM manipulation


//React Fibre :-

// => The new core algorithm of React introduced in React 16.

// The goal of React Fiber is to increase its suitability for areas like animation, layout, and gestures. Its headline feature is incremental rendering: the ability to split rendering work into chunks and spread it out over multiple frames.

// Features:-
// --> Its headline feature is incremental rendering: the ability to split rendering work into chunks and spread it out over multiple frames.
// --> Other key features include the ability to pause, abort, or reuse work as new updates come in
// --> the ability to assign priority to different types of updates
// -->  new concurrency primitives.